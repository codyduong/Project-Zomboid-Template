# This is the template repo's OWN .gitignore (for when it's used standalone / for dev on
# the scripts themselves). templates/.gitignore is the separate file that gets scaffolded
# into new mod repos — don't conflate the two.
.DS_Store
Thumbs.db

# npm deps for scripts/set-workshop-tags.mjs (package.json/package-lock.json are committed;
# node_modules is installed on demand by the set-workshop-tags mise task)
node_modules/
